import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Sparkles.Moble.Ionic.Vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
